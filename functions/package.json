{
  "name": "ojas-functions",
  "description": "OJAS Firebase backend functions for secure server-side push notifications and media processing orchestration.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "deploy": "firebase deploy --only functions --project ojas-e8161 --non-interactive"
  },
  "dependencies": {
    "@azure/storage-queue": "^12.26.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
